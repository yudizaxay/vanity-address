---
description: vanity-address project context — read AGENTS.md for full memory
alwaysApply: true
---

# vanity-address — session context

**Read first:** [AGENTS.md](../../AGENTS.md) for full project memory, release checklist, and pending work.

## Quick facts

- Rust workspace: `vanity-core` (engine) + `vanity-address` (CLI) + `vanity-wasm` + `vanity-app` (Tauri desktop)
- **v0.4.1** live on crates.io / GitHub / Homebrew / npm; chain logic only in `vanity-core`
- Publish order: `vanity-core` then `vanity-address` (no `#` on same shell line)
- npm: `prepare-npm.sh` + `build-wasm.sh` then `publish-npm.sh`
- Windows CLI: `terminal.rs` Press-only keys; `menu.rs` crossterm clear
- crates.io README needs absolute GitHub/raw URLs for images and links

## Before releases

`make check-ci` → version bump all packages → `CHANGELOG.md` → Homebrew formula → tag → `cargo publish` → sync Homebrew tap → `prepare-npm` + `publish-npm`

## Docs map

- `docs/INSTALL.md` — install all platforms
- `docs/USAGE.md` — CLI usage, chains, JSON
- `docs/HOMEBREW.md` — Homebrew tap
- `docs/NPM.md` — npm CLI wrapper
- `RELEASING.md` — maintainer release flow
